  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %>
    <div class="form_input"><%= f.text_field :username %></div>
  </div>
  <div class="field">
    <%= f.label :first_name %>
    <div class="form_input"><%= f.text_field :first_name %></div>
  </div>
  <div class="field">
    <%= f.label :last_name %>
    <div class="form_input"><%= f.text_field :last_name %></div>
  </div>
  <div class="field">
    <%= f.label :role %>
    <div class="form_input"><%= f.select(:role, options_for_select([["none","none"],["admin","admin"]],(@user.role? ? @user.role : "none")))%></div>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>


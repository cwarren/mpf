Mobile Presence Framework

TODO

CURRENT

add google tracking code
    - basic tracking code on page load (JS in header)
    - on embed and rss elements, track list item clicks
        + http://code.google.com/intl/en/apis/analytics/docs/tracking/asyncUsageGuide.html
        + <li onclick="_gaq.push(['_trackPageview', '/<%=item.urltitle%>'])">blah</li>

consider text/code-based embedding instead of iframe embedding

create special 'apps' package?

admin/service_package/edit/
    - available service packages aren't showing the package background (img/package.PNG is not loading)
        + name wrong?
        + location wrong?

allow header images for packages?
    - create table fields
    - display it if it exists
        + handle sizing for tablets
